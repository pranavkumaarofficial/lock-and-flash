# 📂 FILE PLACEMENT GUIDE

## Complete Project Structure

```
ScreenTracker/                                    ← Project root folder
│
├── 📄 build.gradle.kts                          ← PROJECT LEVEL (from build.gradle.kts.root)
├── 📄 settings.gradle.kts                       ← PROJECT LEVEL
├── 📄 gradle.properties                         ← PROJECT LEVEL
├── 📄 README.md                                 ← Documentation
├── 📄 QUICKSTART.md                             ← Quick start guide
│
├── 📁 gradle/                                    ← Auto-generated by Android Studio
│   └── wrapper/
│       ├── gradle-wrapper.jar
│       └── gradle-wrapper.properties
│
└── 📁 app/                                       ← App module folder
    │
    ├── 📄 build.gradle.kts                      ← APP LEVEL (from build.gradle.kts)
    ├── 📄 proguard-rules.pro                    ← Auto-generated
    │
    └── 📁 src/
        │
        └── 📁 main/
            │
            ├── 📄 AndroidManifest.xml            ← CONFIGURATION FILE
            │
            └── 📁 java/
                │
                └── 📁 com/
                    │
                    └── 📁 screentracker/
                        │
                        └── 📁 app/               ← Package folder
                            │
                            ├── 📱 MainActivity.kt          ← KOTLIN FILE 1
                            ├── ⚙️ MonitoringService.kt     ← KOTLIN FILE 2
                            └── 💾 Database.kt              ← KOTLIN FILE 3
```

---

## 📝 Step-by-Step File Placement

### Phase 1: Create New Project in Android Studio

1. **File** → **New** → **New Project**
2. Select **Empty Activity (Compose)**
3. Configure:
   - Name: `ScreenTracker`
   - Package: `com.screentracker.app`
   - Save location: Choose your preferred location
   - Language: `Kotlin`
   - Minimum SDK: `API 26`
   - Build configuration: `Kotlin DSL`
4. Click **Finish**

Android Studio creates the basic structure.

---

### Phase 2: Replace Root-Level Files

Navigate to the **project root** (where you can see the `app` folder):

```
ScreenTracker/
├── ❌ DELETE: build.gradle.kts (the one Android Studio created)
└── ✅ REPLACE WITH: build.gradle.kts.root → RENAME to build.gradle.kts
```

**Then replace these existing files:**

| File | Location | Action |
|------|----------|--------|
| `settings.gradle.kts` | Root | Replace content |
| `gradle.properties` | Root | Replace content |

---

### Phase 3: Replace App Module Files

Navigate to: `ScreenTracker/app/`

```
app/
├── ❌ DELETE: build.gradle.kts (the one Android Studio created)
└── ✅ REPLACE WITH: build.gradle.kts (from downloaded files)
```

Navigate to: `ScreenTracker/app/src/main/`

```
main/
├── ❌ DELETE: AndroidManifest.xml (the one Android Studio created)
└── ✅ REPLACE WITH: AndroidManifest.xml (from downloaded files)
```

---

### Phase 4: Add Kotlin Source Files

Navigate to: `ScreenTracker/app/src/main/java/com/screentracker/app/`

You'll see Android Studio already created a `MainActivity.kt` file.

**Replace and add files:**

| File | Action | Content |
|------|--------|---------|
| `MainActivity.kt` | ❌ DELETE & ✅ RECREATE | Copy MainActivity.kt content |
| `MonitoringService.kt` | ✅ CREATE NEW | Copy MonitoringService.kt content |
| `Database.kt` | ✅ CREATE NEW | Copy Database.kt content |

**How to create new Kotlin files:**
1. Right-click on `com.screentracker.app` package
2. New → Kotlin Class/File
3. Choose "File"
4. Name it (e.g., `MonitoringService`)
5. Paste the content

---

### Phase 5: Add Documentation (Optional)

In the project root, create:

```
ScreenTracker/
├── 📄 README.md          ← Comprehensive documentation
└── 📄 QUICKSTART.md      ← Quick start guide
```

---

## 🎯 Quick Reference Map

### Where Each Downloaded File Goes:

| Downloaded File | Goes To | Full Path |
|----------------|---------|-----------|
| `MainActivity.kt` | App package | `app/src/main/java/com/screentracker/app/MainActivity.kt` |
| `MonitoringService.kt` | App package | `app/src/main/java/com/screentracker/app/MonitoringService.kt` |
| `Database.kt` | App package | `app/src/main/java/com/screentracker/app/Database.kt` |
| `AndroidManifest.xml` | Main folder | `app/src/main/AndroidManifest.xml` |
| `build.gradle.kts` (app) | App folder | `app/build.gradle.kts` |
| `build.gradle.kts.root` | Root folder | `build.gradle.kts` ← RENAME IT |
| `settings.gradle.kts` | Root folder | `settings.gradle.kts` |
| `gradle.properties` | Root folder | `gradle.properties` |

---

## ✅ Verification Checklist

After placing all files:

### In Project Root:
- [ ] `build.gradle.kts` exists (content from build.gradle.kts.root)
- [ ] `settings.gradle.kts` exists
- [ ] `gradle.properties` exists

### In `app/` folder:
- [ ] `build.gradle.kts` exists (content from app build.gradle.kts)

### In `app/src/main/`:
- [ ] `AndroidManifest.xml` exists

### In `app/src/main/java/com/screentracker/app/`:
- [ ] `MainActivity.kt` exists
- [ ] `MonitoringService.kt` exists
- [ ] `Database.kt` exists

---

## 🔄 After Placing Files

1. **Sync Gradle:** Click the elephant icon 🐘 "Sync Project with Gradle Files"
2. **Wait:** Let Gradle download dependencies (2-5 minutes)
3. **Check:** Look for "BUILD SUCCESSFUL" message at bottom
4. **Fix Imports:** If any red underlines, press `Alt+Enter` to import

---

## 🚨 Common Mistakes

### ❌ Wrong: Putting files in wrong package
```
app/src/main/java/
└── MainActivity.kt  ← WRONG! Missing package folders
```

### ✅ Correct: Proper package structure
```
app/src/main/java/com/screentracker/app/
└── MainActivity.kt  ← CORRECT! In proper package
```

---

### ❌ Wrong: Not renaming build.gradle.kts.root
```
ScreenTracker/
├── build.gradle.kts.root  ← WRONG! Keep this name
```

### ✅ Correct: Renamed to build.gradle.kts
```
ScreenTracker/
├── build.gradle.kts  ← CORRECT! Renamed from build.gradle.kts.root
```

---

## 💡 Pro Tips

1. **Use Android Studio's File Explorer:**
   - Switch to "Android" view (not "Project" view)
   - Files are organized by type, easier to navigate

2. **Copy-Paste in Android Studio:**
   - Open file in text editor
   - Copy all content
   - In Android Studio: right-click file → delete → create new → paste

3. **Package Auto-Creation:**
   - If `com/screentracker/app` folders don't exist
   - Android Studio creates them when you create a file

4. **Gradle Sync:**
   - Always sync after changing build.gradle files
   - File → Sync Project with Gradle Files

---

## 🆘 Troubleshooting File Issues

### "Package does not exist"
→ Check package declaration at top of Kotlin files:
```kotlin
package com.screentracker.app  // Must match your package name
```

### "Cannot resolve symbol"
→ Sync Gradle again and reimport:
- File → Invalidate Caches → Invalidate and Restart

### "Build failed"
→ Check file locations match exactly as shown above

---

## 📸 Visual Guide

**Android View (Recommended):**
```
📁 app
  📁 manifests
    📄 AndroidManifest.xml  ← Place here
  📁 java
    📁 com.screentracker.app
      📄 MainActivity.kt
      📄 MonitoringService.kt
      📄 Database.kt
  📄 build.gradle.kts  ← App-level
📄 build.gradle.kts  ← Project-level
📄 settings.gradle.kts
📄 gradle.properties
```

**Project View:**
```
📁 ScreenTracker
  📁 app
    📁 src
      📁 main
        📁 java
          📁 com
            📁 screentracker
              📁 app
                📄 MainActivity.kt
                📄 MonitoringService.kt
                📄 Database.kt
        📄 AndroidManifest.xml
    📄 build.gradle.kts
  📄 build.gradle.kts
  📄 settings.gradle.kts
  📄 gradle.properties
```

---

**Your files are now properly organized! Ready to build! 🚀**
